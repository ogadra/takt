# =============================================================================
# TAKT UI Labels — English
# =============================================================================
# User-facing display strings (not AI prompts).
# Template variables use {variableName} syntax.
# =============================================================================

# ===== Interactive Mode UI =====
interactive:
  sourceContextLabel: "Source Context"
  conversationLabel: "Conversation:"
  noTranscript: "(No local transcript. Summarize the current session context.)"
  ui:
    intro: "Interactive mode - describe your task. Commands: /go (create instruction & run), /play (run now), /accept (use latest assistant response), /paste-image (attach clipboard image), /resume (load session), /cancel (exit)"
    introQuiet: "Quiet mode - describe your task. Instructions will be generated without further questions."
    introPassthrough: "Passthrough mode - describe your task. Your input will be passed directly as the task."
    resume: "Resuming previous session"
    noConversation: "No conversation yet. Please describe your task first."
    thinking: "Assistant is thinking..."
    creatingInstruction: "Creating instruction..."
    summarizeFailed: "Failed to summarize conversation. Please try again."
    continuePrompt: "Okay, continue describing your task."
    proposed: "Proposed task instruction:"
    actionPrompt: "What would you like to do?"
    actions:
      execute: "Execute now"
      createIssue: "Create Issue"
      saveTask: "Save as Task"
      continue: "Continue editing"
    cancelled: "Cancelled"
    acceptNoAssistant: "No assistant response found. Please describe your task first."
    playNoTask: "Please specify task content: /play <task>"
    retryNoOrder: "No previous order (order.md) found. /retry is only available during retry."
    retryUnavailable: "/retry is only available in Retry mode from `takt list`."
    pasteImageUnavailable: "/paste-image is only available for image attachment during interactive input."
    personaFallback: "No persona available for the first step. Falling back to assistant mode."
  modeSelection:
    prompt: "Select interactive mode:"
    assistant: "Assistant"
    assistantDescription: "Ask clarifying questions before generating instructions"
    persona: "Persona"
    personaDescription: "Converse as the first agent's persona"
    quiet: "Quiet"
    quietDescription: "Generate instructions without asking questions"
    passthrough: "Passthrough"
    passthroughDescription: "Pass your input directly as task text"
  runSelector:
    confirm: "Reference a previous run's results?"
    prompt: "Select a run to reference:"
    noRuns: "No previous runs found."
  continueNoSession: "No previous assistant session found. Starting a new session."
  resumeSessionLoaded: "Session loaded. Subsequent AI calls will use this session."
  sessionSelector:
    prompt: "Resume from a recent session?"
    newSession: "New session"
    newSessionDescription: "Start a fresh conversation"
    noSessions: "No resumable sessions were found. Starting a new session."
    lastResponse: "Last: {response}"
    messages: "{count} messages"
  previousTask:
    success: "✅ Previous task completed successfully"
    error: "❌ Previous task failed: {error}"
    userStopped: "⚠️ Previous task was interrupted by user"
    workflow: "Workflow: {workflowName}"
    timestamp: "Executed: {timestamp}"
  commands:
    accept: "Accept latest assistant response"
    play: "Run a task immediately"
    go: "Create instruction & run"
    retry: "Review & rerun with previous instructions"
    replay: "Rerun immediately with previous instructions"
    cancel: "Exit interactive mode"
    resume: "Load a previous session"
    pasteImage: "Attach clipboard image"
    setup: "Configure exec agents"

# ===== Exec Mode UI =====
exec:
  common:
    add: "Add"
    back: "Back"
    builtin: "Builtin"
    cancel: "Cancel"
    currentSuffix: "(current)"
    default: "Default"
    delete: "Delete"
    discard: "Discard"
    global: "Global"
    none: "none"
    project: "Project"
    providerDefault: "provider default"
    save: "Save"
    user: "User"
  scope:
    projectDescription: ".takt/facets"
    globalDescription: "~/.takt/facets"
  setup:
    teamConfiguration: "Exec agents"
    assistantSummary: "Assistant agent: {summary}"
    workersSummary: "Worker agents: {count}"
    reviewsSummary: "Review agents: {count}"
    replanSummary: "Replanning agent: {instruction}"
    loopSummary: "Loop detection: {small}/{large}/{max}"
    preset: "Preset"
  actors:
    workers: "Worker agents"
    reviews: "Review agents"
    mustContainOne: "{label} must contain at least one entry."
    deletePrompt: "Delete {label}"
    invalidIndex: "Invalid {label} index: {index}"
  settings:
    assistant: "Assistant agent settings"
    actor: "{name} settings"
    provider: "Provider"
    model: "Model"
    effort: "Effort"
    name: "Name"
    instruction: "Instruction"
    knowledge: "Knowledge"
    policy: "Policy"
    customModel: "Custom input..."
    customModelPrompt: "Custom model"
    customModelRequired: "Custom model must be a non-empty string."
    defaultModel: "Default ({value})"
    defaultEffort: "Default (none)"
  fields:
    provider: "Provider: {value}"
    model: "Model: {value}"
    effort: "Effort: {value}"
    name: "Name: {value}"
    instruction: "Instruction: {value}"
    knowledge: "Knowledge: {value}"
    policy: "Policy: {value}"
    smallLoopThreshold: "Small loop detection threshold: {value}"
    largeLoopThreshold: "Large loop detection threshold: {value}"
    maxSteps: "Max steps: {value}"
    actorInstruction: "instruction: {value}"
  replan:
    settings: "Replanning agent settings"
  loop:
    settings: "Loop detection settings"
    smallThresholdPrompt: "Small loop detection threshold"
    largeThresholdPrompt: "Large loop detection threshold"
    maxStepsPrompt: "Max steps"
  preset:
    menu: "Preset"
    load: "Load preset"
    saveCurrent: "Save current preset"
    delete: "Delete preset"
    saveScope: "Preset save scope"
    deleteScope: "Preset delete scope"
    loadSource: "Preset load source"
    defaultDescription: "Default exec configuration"
    builtinDescription: "builtins/exec/presets"
    projectDescription: ".takt/exec/presets"
    globalDescription: "~/.takt/exec/presets"
    loadFromSource: "Load {source} preset"
    deleteFromSource: "Delete {source} preset"
    namePrompt: "Preset name"
    descriptionPrompt: "Preset description"
    descriptionDefault: "Custom exec preset"
    saved: "Saved {scope} exec preset: {name}"
    deleted: "Deleted {scope} exec preset: {name}"
    exportAsWorkflow: "Export preset as workflow"
    exportNamePrompt: "Workflow name"
    exported: "Exported workflow: {name}"
  facets:
    instructions: "instructions"
    knowledge: "knowledge"
    policies: "policies"
    selectPrompt: "Select {kind} facet"
    togglePrompt: "Toggle {kind} facet"
    listPrompt: "{kind} facets"
    saveScope: "Facet save scope"
    editedSaveScope: "Edited facet save scope"
    saveGenerated: "Save generated facet?"
    generated: "Generated {kind} facet \"{name}\":"
    created: "Created {scope} {kind} facet: {name}"
    consultationPrompt: "Describe the {kind} facet changes for {name}: "
    namePrompt: "Facet name"
    instructionMenu: "Instruction facet"
    selectExisting: "Select existing ({current})"
    editWithAI: "Edit with AI"
    createWithAI: "Create with AI"
    createWithEditor: "Create with editor"
    openInEditor: "Open in editor"
    restoreDefault: "Restore default ({defaultRef})"
    toggleExisting: "Toggle existing"
    clearAll: "Clear all"

# ===== Workflow Execution UI =====
workflow:
  teamLeader:
    decomposeWait: "Team leader is decomposing tasks. This may take some time..."
    feedbackWait: "Team leader is reviewing results and planning next tasks..."
  iterationLimit:
    maxReached: "Reached max iterations ({currentIteration}/{maxSteps})"
    currentStep: "Current step: {currentStep}"
    continueQuestion: "Continue?"
    continueLabel: "Continue (enter additional iterations)"
    continueDescription: "Increase the limit by the entered count"
    stopLabel: "Stop"
    inputPrompt: "Enter additional iterations (>= 1)"
    invalidInput: "Please enter an integer of 1 or greater."
    userInputPrompt: "Enter additional instructions (empty to cancel)"
  notifyComplete: "Workflow complete ({iteration} iterations)"
  notifyAbort: "Aborted: {reason}"
  sigintGraceful: "Ctrl+C: Aborting workflow..."
  sigintTimeout: "Graceful shutdown timed out after {timeoutMs}ms"
  sigintForce: "Ctrl+C: Force exit"

# ===== Instruct Mode UI (takt list -> instruct) =====
instruct:
  ui:
    intro: "Instruct mode - describe additional instructions. Commands: /go (create instruction & run), /replay (resubmit previous order), /cancel (exit)"
    resume: "Resuming previous session"
    noConversation: "No conversation yet. Please describe your instructions first."
    summarizeFailed: "Failed to create instruction. Please try again."
    continuePrompt: "Okay, continue describing your instructions."
    proposed: "Proposed additional instructions:"
    actionPrompt: "What would you like to do?"
    actions:
      execute: "Execute now"
      saveTask: "Save as Task"
      continue: "Continue editing"
    cancelled: "Cancelled"
    replayNoOrder: "Previous order (order.md) not found"

# ===== Retry Mode UI (takt list -> retry) =====
retry:
  ui:
    intro: "Retry mode - describe additional instructions. Commands: /go (create instruction & run), /retry (rerun previous order), /cancel (exit)"
  usePreviousWorkflowConfirm: "Use previous workflow \"{workflow}\"?"

run:
  notifyComplete: "Run complete ({total} tasks)"
  notifyAbort: "Run finished with errors ({failed})"

# ===== Issue Creation UI =====
issue:
  labelSelection:
    prompt: "Label:"
    none: "None"
    custom: "Custom input"
    customPrompt: "Enter label name"
